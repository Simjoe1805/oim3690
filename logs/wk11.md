# weekly log: 11



## what I learned this week 
- arrays and for loops 
- using APIs as example 
- loops
- common loop patterns start with total = 0, then add each element. This works for any cumulative calculation. 
- i++ means changing 0 to 1  
- business usecase of i++ or common loop pattern. 
- for...of loop, it i a cleaner way to loop through arrays when you do not need the index. 

eg. const fruits = ['apple', 'banana', 'cherry']; 
Classic for loop 
for (let i = 0; i <fruits.length; i++) {
    console.log(fruits[i])
}

 



## code I am proud of 
const growthRates = [];
  for (let i = 1; i <= nYears; i++) {
    const g = parseFloat(document.getElementById(`dcfGrowth${i}`)?.value);
    growthRates.push(isNaN(g) ? 0.05 : g / 100);
  } 
  - what this code does is it helps build my dcf model for my miniproject2 assignment for class, which is for an app that I built, used to create an investment recommendation based on some basic financial metrics and also models that the app can create. What this function does is that it reads the user’s growth assumption for each future year, which is then used to build the discounted cash flow model for analysis. 

## Challenges I faced 
- The challenges I faced was using the for loops, since most of them are vibe coded. Trying to understand what it does is pretty complex, because some of the functions are very complex looking, but with asking the AI, I could slowly understand the implications better. 


## AI usage 
The usage of AI is pretty high, using it to vibecode the javascript of my mini project 2. 



