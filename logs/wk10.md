# weekly log: 10



## what I learned this week 
- floatingpoint number: number with decimal part, although they are integers. 
- parsefloat 
- for naming conventions, you use camelCase for variables like userName, and totalScore 
- Upper Case for constants, etc. 
- data types: primitive (boolean, undefined, string, number) 
- template literals 
- arithmetic operators 
- combine conditions function && (both must be true) || either can be true, ! reverse 
- Javascript conditionals: conditionals let your code make decisions if user is logged in. 
- Basic if: conditions must be in parenthesis. 
- if else: use it when you have exactly two paths. 
- if else if condition is met, end of story. 
 



## code I am proud of 
if (config.apiKey.toLowerCase() === "demo" && config.symbol !== "IBM") {
- It is a basic if statement, so the condition is inside parentheses.
It uses &&, which means both conditions must be true.
Condition 1: the API key is "demo".


## Challenges I faced 

- the challenges that I faced is based on the APIs that I do not know how to implement it yet. 

## AI usage 
- the AI tool codex is used to help build the app. 



