# weekly log: 12



## what I learned this week 
- review of API = Application pogramming interface 
- common APIs include weather API, Movie API, Github APi, currency API, etc. 
- what is API key? X-Token from the last class example is an API key. 
- JSON = javascript object notation = text format for data 
- fetch () 
- await = we want this function to wait until the data arrives. 
- 
 



## code I am proud of 
There const res = await fetch(url, { headers: { "Accept-Language": "en" } });
if (!res.ok) throw new Error("geocode failed");
...
} catch {
  const label = `${latitude.toFixed(4)}, ${longitude.toFixed(4)}`;
  applyLocation(latitude, longitude, accuracy, label, "device");
}


## Challenges I faced 
There are not a lot of challenges, more towards the overall understanding of what the code entails.
## AI usage 

The AI Usage is high, since most of the codes for the final project is vibecoded. 
