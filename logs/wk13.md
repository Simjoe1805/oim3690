# weekly log: 13



## what I learned this week 
- Working on final project
- touching base on further learning in API 
- reiterating my project to improve the UI and also trying to implement supabase into it.  

 



## code I am proud of 
const liveMap = new maplibregl.Map({
  container: "map",
  style: "https://tiles.openfreemap.org/styles/liberty",
  center: [-71.0589, 42.3601],
  zoom: 13,
});

liveMap.on("load", () => {
  const loc = state.userLocation;
  if (loc && loc.source !== "fallback") {
    liveMap.flyTo({ center: [loc.longitude, loc.latitude], zoom: 14, essential: true, duration: 1400 });
  }
});

proud of the API implementation code because it integrated a real third-party map style service cleanly.


## Challenges I faced 
There are not a lot of challenges, more towards the overall understanding of what the code entails. I am feeling more comfortable with the API usage and also with handling UI 
## AI usage 

The AI Usage is high, since most of the codes for the final project is vibecoded. 
